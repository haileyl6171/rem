# Python deps for the Modal pipeline image.
# Pin versions before the demo once things work.

modal                 # orchestration + GPU hosting + the trigger endpoint
supabase              # DB writes (db.py) + Storage I/O (storage.py)
google-genai          # agents/ (Gemini text + photo vision) AND veo.py (Veo 3 video generation)
mcp                    # agents/creative.py — MCP client to the Pika MCP server (creative vision)
fastapi               # required by @modal.fastapi_endpoint

# --- reconstruction (P4) ---
# gsplat needs a CUDA build toolchain — see the image note in app.py.
# Add the trainer you choose, e.g.:
# gsplat
# torch
# numpy
# opencv-python-headless     # frame I/O / image handling
#
# COLMAP itself is installed via apt in app.py, not pip.
